## Program 11: Write a program to convert ASCII to character.
```
#include<stdio.h>
int main()
{
    int ascii;
    printf("Enter the ASCII value");
    scanf("\n%d",&ascii);
    printf("\nascii to char=%c",ascii);
    return 0;
}
```
**output**
ascii to char=W
